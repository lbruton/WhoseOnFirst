# WhoseOnFirst - Python Dependencies
# Production requirements

# Web Framework
fastapi==0.141.1
uvicorn[standard]==0.52.2
python-multipart==0.0.32  # Required for UploadFile (file import endpoint); 0.0.27 fixes DoS via unbounded multipart headers (Dependabot #8)

# Database
sqlalchemy==2.0.52
alembic==1.19.1  # Database migrations

# Scheduling
apscheduler==3.11.3
pytz==2026.3.post1

# SMS Provider
twilio==9.11.0

# Data Validation
pydantic==2.13.4
pydantic-settings==2.15.0

# Utilities
python-dotenv==1.2.2
httpx==0.28.1  # Async HTTP client

# Security
argon2-cffi==25.1.0  # Argon2id password hashing (OWASP 2025 recommended)
cryptography>=50.0.0,<51  # Fernet + PBKDF2 for at-rest encryption of Twilio auth tokens (WHO-43); floor bumped past CVE-flagged ranges (CodeRabbit)

# Logging
python-json-logger==4.1.0
