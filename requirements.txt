# WhoseOnFirst - Python Dependencies
# Production requirements

# Web Framework
fastapi==0.139.0
uvicorn[standard]==0.50.0
python-multipart==0.0.32  # Required for UploadFile (file import endpoint); 0.0.27 fixes DoS via unbounded multipart headers (Dependabot #8)

# Database
sqlalchemy==2.0.51
alembic==1.18.5  # Database migrations

# Scheduling
apscheduler==3.11.3
pytz==2026.2

# SMS Provider
twilio==9.10.9

# Data Validation
pydantic==2.13.4
pydantic-settings==2.14.2

# Utilities
python-dotenv==1.2.2
httpx==0.28.1  # Async HTTP client

# Security
argon2-cffi==25.1.0  # Argon2id password hashing (OWASP 2025 recommended)
cryptography>=49.0.0,<50  # Fernet + PBKDF2 for at-rest encryption of Twilio auth tokens (WHO-43); floor bumped past CVE-flagged ranges (CodeRabbit)

# Logging
python-json-logger==4.1.0
